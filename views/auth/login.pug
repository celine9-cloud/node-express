extends ../layout

block content
  .page-shell.narrow
    h1 로그인
    p.description 매출, 입금 예정, 리뷰 알림을 확인하려면 로그인하세요.

    if errors && errors.length
      ul.alert
        each error in errors
          li= error

    form.card-form(method='post', action='/auth/login')
      input(type='hidden', name='next', value=values.next || '')
      label
        | 이메일
        input(type='email', name='email', value=values.email || '', required, autocomplete='email')
      label
        | 비밀번호
        input(type='password', name='password', required, autocomplete='current-password')
      button.primary-button(type='submit') 로그인

    .social-login
      a.kakao-button(href=`/auth/terms?provider=kakao${values.next ? '&next=' + encodeURIComponent(values.next) : ''}`)
        span.kakao-mark K
        span 카카오로 계속하기

    p.muted
      | 계정이 없나요?
      |
      a(href='/auth/signup') 회원가입
